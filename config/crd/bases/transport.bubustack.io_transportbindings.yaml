---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: transportbindings.transport.bubustack.io
spec:
  group: transport.bubustack.io
  names:
    kind: TransportBinding
    listKind: TransportBindingList
    plural: transportbindings
    singular: transportbinding
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: TransportBinding is the Schema for the transportbindings API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of TransportBinding
            properties:
              downstreams:
                description: downstreams enumerates the downstream peers that consume
                  from this step.
                items:
                  description: TransportPeer describes an upstream or downstream participant
                    in the transport graph.
                  properties:
                    name:
                      description: name is the logical name of the peer (typically
                        the engram name).
                      minLength: 1
                      type: string
                    port:
                      default: 50051
                      description: port is the TCP port exposed by the peer Service.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    service:
                      description: service is the Kubernetes Service backing the peer.
                      minLength: 1
                      type: string
                  required:
                  - name
                  - service
                  type: object
                type: array
              engramName:
                description: engramName is the run-specific engram backing the step.
                minLength: 1
                type: string
              stepName:
                description: stepName is the Story step that owns this binding.
                minLength: 1
                type: string
              storyRunRef:
                description: |-
                  storyRunRef identifies the StoryRun this binding belongs to when the binding is scoped to an individual run.
                  Story-scoped infrastructure bindings omit this field until a concrete StoryRun exists.
                properties:
                  name:
                    description: |-
                      Name of the referenced object.
                      Must be a valid DNS-1123 label as defined by Kubernetes naming conventions.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  namespace:
                    description: |-
                      Namespace of the referenced object.
                      If empty, defaults to same namespace as the referencing object.

                      Cross-namespace references should be used carefully and only when
                      multi-tenancy is properly configured. Consider the security implications:
                      - Ensure RBAC policies allow cross-namespace access
                      - Validate that the target namespace is trusted
                      - Monitor cross-namespace references for security compliance
                    maxLength: 63
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  uid:
                    description: |-
                      UID of the referenced StoryRun for drift detection.
                      This field is populated by controllers and should not be set by users.

                      StoryRun drift detection helps maintain execution integrity and
                      enables proper cleanup of orphaned StepRuns.
                    type: string
                required:
                - name
                type: object
              transport:
                description: transport indicates which transport primitive should
                  be used.
                enum:
                - grpc
                type: string
              upstreams:
                description: upstreams enumerates the upstream peers this step consumes
                  from.
                items:
                  description: TransportPeer describes an upstream or downstream participant
                    in the transport graph.
                  properties:
                    name:
                      description: name is the logical name of the peer (typically
                        the engram name).
                      minLength: 1
                      type: string
                    port:
                      default: 50051
                      description: port is the TCP port exposed by the peer Service.
                      format: int32
                      maximum: 65535
                      minimum: 1
                      type: integer
                    service:
                      description: service is the Kubernetes Service backing the peer.
                      minLength: 1
                      type: string
                  required:
                  - name
                  - service
                  type: object
                type: array
            required:
            - engramName
            - stepName
            - transport
            type: object
          status:
            description: status defines the observed state of TransportBinding
            properties:
              conditions:
                description: |-
                  conditions represent the current state of the TransportBinding resource.
                  Each condition has a unique type and reflects the status of a specific aspect of the resource.

                  Standard condition types include:
                  - "Ready": the binding has been fully resolved
                  - "Resolving": the transport controller is computing endpoints
                  - "Error": reconciliation failed

                  The status of each condition is one of True, False, or Unknown.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              downstreamHost:
                description: downstreamHost is the resolved downstream endpoint (host:port)
                  for the engram owning this binding.
                type: string
              observedGeneration:
                description: observedGeneration records the generation observed by
                  the transport controller.
                format: int64
                type: integer
              upstreamHost:
                description: upstreamHost is the resolved upstream endpoint (host:port)
                  for the engram owning this binding.
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
