---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.0
  name: stories.bubustack.io
spec:
  group: bubustack.io
  names:
    categories:
    - bubu
    - ai
    - workflows
    kind: Story
    listKind: StoryList
    plural: stories
    shortNames:
    - story
    singular: story
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.stepsTotal
      name: Steps
      type: integer
    - jsonPath: .status.validationStatus
      name: Status
      type: string
    - jsonPath: .status.usageCount
      name: Usage
      type: integer
    - jsonPath: .status.triggers
      name: Triggers
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          Story defines a workflow that coordinates Engrams and primitive steps.
          It captures orchestration logic, dependency ordering, and configuration that is
          shared across every StoryRun derived from the Story.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Story
            properties:
              inputsSchema:
                description: InputsSchema defines the schema for the data required
                  to start a StoryRun.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              output:
                description: |-
                  Output defines a template for the Story's final output.
                  This CEL expression is evaluated upon successful completion of the story,
                  and the result is stored in the StoryRun's status.output field.
                  It has access to 'inputs' and 'steps' contexts.
                  e.g., {"final_message": "processed " + steps.load_data.outputs.count + " records"}
                type: object
                x-kubernetes-preserve-unknown-fields: true
              outputsSchema:
                description: OutputsSchema defines the schema for the data this Story
                  is expected to produce.
                type: object
                x-kubernetes-preserve-unknown-fields: true
              pattern:
                default: batch
                description: |-
                  Pattern selects the execution model. Batch stories produce short-lived StoryRuns;
                  streaming stories maintain long-lived streaming topologies.
                enum:
                - batch
                - streaming
                type: string
              policy:
                description: Policy provides optional story-wide defaults such as
                  timeouts and storage.
                properties:
                  concurrency:
                    description: Concurrency limits
                    format: int32
                    type: integer
                  execution:
                    description: Default execution settings (can be overridden at
                      step level)
                    properties:
                      job:
                        description: Job enumerates the job controller settings.
                        properties:
                          backoffLimit:
                            description: Backoff limit for jobs
                            format: int32
                            type: integer
                          restartPolicy:
                            description: Restart policy
                            type: string
                          storyRunRetentionSeconds:
                            description: |-
                              Retention period for the StoryRun record itself after it finishes.
                              After this period expires, the StoryRun is deleted to prevent etcd overload.
                              Set to 0 to keep StoryRuns forever (requires manual cleanup).
                              Set to -1 to delete StoryRun immediately after child cleanup (not recommended).
                              Recommended: 86400 (24 hours) to 604800 (7 days) for production systems.
                              Default: 86400 (24 hours)
                            format: int32
                            type: integer
                          ttlSecondsAfterFinished:
                            description: |-
                              TTL for cleaning up child resources (StepRuns, Pods) after StoryRun finishes.
                              After this period expires, child resources are deleted to free up cluster resources.
                              The parent StoryRun record is preserved for observability.
                              Default: 3600 (1 hour)
                            format: int32
                            type: integer
                        type: object
                      realtime:
                        description: Realtime enumerates streaming-specific execution
                          settings (e.g., TTL for lingering pods).
                        properties:
                          ttlSecondsAfterFinished:
                            description: |-
                              TTLSecondsAfterFinished controls how long realtime resources (Deployments, Services) remain after finish.
                              Set to 0 to delete immediately, negative to keep indefinitely, or positive seconds to keep temporarily.
                            format: int32
                            type: integer
                        type: object
                      resources:
                        description: Resources enumerates the concrete pod resource
                          assignments.
                        properties:
                          cpuLimit:
                            description: CPU limit
                            type: string
                          cpuRequest:
                            description: CPU request
                            type: string
                          memoryLimit:
                            description: Memory limit
                            type: string
                          memoryRequest:
                            description: Memory request
                            type: string
                        type: object
                      retry:
                        description: Retry enumerates the retry configuration.
                        properties:
                          backoff:
                            description: |-
                              Backoff strategy for retry delays
                              - exponential: 1s, 2s, 4s, 8s... (doubles each time)
                              - linear: 1s, 2s, 3s, 4s... (increases by delay each time)
                              - constant: 1s, 1s, 1s, 1s... (same delay each time)
                            enum:
                            - exponential
                            - linear
                            - constant
                            type: string
                          delay:
                            default: 1s
                            description: |-
                              Base delay between retries
                              Examples: "1s", "5s", "30s"
                            type: string
                          maxRetries:
                            default: 3
                            description: Maximum number of retry attempts
                            format: int32
                            maximum: 10
                            minimum: 0
                            type: integer
                        type: object
                      security:
                        description: Security enumerates the concrete pod security
                          settings.
                        properties:
                          allowPrivilegeEscalation:
                            description: Whether to allow privilege escalation
                            type: boolean
                          readOnlyRootFilesystem:
                            description: Read-only root filesystem
                            type: boolean
                          runAsNonRoot:
                            description: Whether to run as non-root user
                            type: boolean
                          runAsUser:
                            description: User ID to run as
                            format: int64
                            type: integer
                        type: object
                      serviceAccountName:
                        description: ServiceAccountName identifies the ServiceAccount
                          used by derived workloads.
                        type: string
                      storage:
                        description: Storage enumerates the resolved storage policy
                          applied to the workload.
                        properties:
                          file:
                            description: File specifies configuration for a filesystem-backed
                              object store.
                            properties:
                              emptyDir:
                                description: |-
                                  EmptyDir defines an ephemeral volume to provision for file storage. When both
                                  EmptyDir and VolumeClaimName are omitted, an EmptyDir{} volume is used by default.
                                properties:
                                  medium:
                                    description: |-
                                      medium represents what type of storage medium should back this directory.
                                      The default is "" which means to use the node's default medium.
                                      Must be an empty string (default) or Memory.
                                      More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                                    type: string
                                  sizeLimit:
                                    anyOf:
                                    - type: integer
                                    - type: string
                                    description: |-
                                      sizeLimit is the total amount of local storage required for this EmptyDir volume.
                                      The size limit is also applicable for memory medium.
                                      The maximum usage on memory medium EmptyDir would be the minimum value between
                                      the SizeLimit specified here and the sum of memory limits of all containers in a pod.
                                      The default is nil which means that the limit is undefined.
                                      More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                                    pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                    x-kubernetes-int-or-string: true
                                type: object
                              path:
                                description: |-
                                  Path is the directory inside the container where the storage backend should write files.
                                  Defaults to "/var/run/bubu/storage" when omitted.
                                type: string
                              volumeClaimName:
                                description: |-
                                  VolumeClaimName references an existing PersistentVolumeClaim mounted to the workload.
                                  Mutually exclusive with EmptyDir.
                                type: string
                            type: object
                          s3:
                            description: S3 specifies the configuration for an S3-compatible
                              object store.
                            properties:
                              authentication:
                                description: Authentication configures how the workload
                                  authenticates with S3.
                                properties:
                                  secretRef:
                                    description: |-
                                      SecretRef references a Kubernetes Secret that contains the credentials.
                                      The secret must contain keys 'accessKeyID' and 'secretAccessKey'.
                                    properties:
                                      name:
                                        default: ""
                                        description: |-
                                          Name of the referent.
                                          This field is effectively required, but due to backwards compatibility is
                                          allowed to be empty. Instances of this type with an empty value here are
                                          almost certainly wrong.
                                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                        type: string
                                    type: object
                                    x-kubernetes-map-type: atomic
                                  serviceAccountAnnotations:
                                    additionalProperties:
                                      type: string
                                    description: |-
                                      ServiceAccountAnnotations allows specifying annotations to be added to the
                                      auto-generated ServiceAccount. This is the recommended approach for
                                      IAM-based authentication on cloud providers (e.g., IRSA on EKS).
                                      The key-value pairs will be added to the ServiceAccount's metadata.annotations.
                                    type: object
                                type: object
                              bucket:
                                description: Bucket is the name of the S3 bucket.
                                type: string
                              endpoint:
                                description: Endpoint is the S3 endpoint URL. Useful
                                  for S3-compatible stores like MinIO.
                                type: string
                              region:
                                description: Region is the AWS region of the bucket.
                                type: string
                              usePathStyle:
                                description: UsePathStyle forces path-style addressing
                                  for S3-compatible providers that require it.
                                type: boolean
                            required:
                            - authentication
                            - bucket
                            type: object
                          timeoutSeconds:
                            description: 'NOTE: GCS, AzureBlob, etc., can be added
                              here in the future.'
                            type: integer
                        type: object
                      timeout:
                        description: Timeout is the execution deadline expressed as
                          a Go duration string.
                        type: string
                    type: object
                  retries:
                    description: Retry behavior
                    properties:
                      continueOnStepFailure:
                        description: Whether to continue story on step failure
                        type: boolean
                      stepRetryPolicy:
                        description: Default retry policy for steps
                        properties:
                          backoff:
                            description: |-
                              Backoff strategy for retry delays
                              - exponential: 1s, 2s, 4s, 8s... (doubles each time)
                              - linear: 1s, 2s, 3s, 4s... (increases by delay each time)
                              - constant: 1s, 1s, 1s, 1s... (same delay each time)
                            enum:
                            - exponential
                            - linear
                            - constant
                            type: string
                          delay:
                            default: 1s
                            description: |-
                              Base delay between retries
                              Examples: "1s", "5s", "30s"
                            type: string
                          maxRetries:
                            default: 3
                            description: Maximum number of retry attempts
                            format: int32
                            maximum: 10
                            minimum: 0
                            type: integer
                        type: object
                    type: object
                  storage:
                    description: Storage configuration
                    properties:
                      file:
                        description: File specifies configuration for a filesystem-backed
                          object store.
                        properties:
                          emptyDir:
                            description: |-
                              EmptyDir defines an ephemeral volume to provision for file storage. When both
                              EmptyDir and VolumeClaimName are omitted, an EmptyDir{} volume is used by default.
                            properties:
                              medium:
                                description: |-
                                  medium represents what type of storage medium should back this directory.
                                  The default is "" which means to use the node's default medium.
                                  Must be an empty string (default) or Memory.
                                  More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                                type: string
                              sizeLimit:
                                anyOf:
                                - type: integer
                                - type: string
                                description: |-
                                  sizeLimit is the total amount of local storage required for this EmptyDir volume.
                                  The size limit is also applicable for memory medium.
                                  The maximum usage on memory medium EmptyDir would be the minimum value between
                                  the SizeLimit specified here and the sum of memory limits of all containers in a pod.
                                  The default is nil which means that the limit is undefined.
                                  More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                            type: object
                          path:
                            description: |-
                              Path is the directory inside the container where the storage backend should write files.
                              Defaults to "/var/run/bubu/storage" when omitted.
                            type: string
                          volumeClaimName:
                            description: |-
                              VolumeClaimName references an existing PersistentVolumeClaim mounted to the workload.
                              Mutually exclusive with EmptyDir.
                            type: string
                        type: object
                      s3:
                        description: S3 specifies the configuration for an S3-compatible
                          object store.
                        properties:
                          authentication:
                            description: Authentication configures how the workload
                              authenticates with S3.
                            properties:
                              secretRef:
                                description: |-
                                  SecretRef references a Kubernetes Secret that contains the credentials.
                                  The secret must contain keys 'accessKeyID' and 'secretAccessKey'.
                                properties:
                                  name:
                                    default: ""
                                    description: |-
                                      Name of the referent.
                                      This field is effectively required, but due to backwards compatibility is
                                      allowed to be empty. Instances of this type with an empty value here are
                                      almost certainly wrong.
                                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                    type: string
                                type: object
                                x-kubernetes-map-type: atomic
                              serviceAccountAnnotations:
                                additionalProperties:
                                  type: string
                                description: |-
                                  ServiceAccountAnnotations allows specifying annotations to be added to the
                                  auto-generated ServiceAccount. This is the recommended approach for
                                  IAM-based authentication on cloud providers (e.g., IRSA on EKS).
                                  The key-value pairs will be added to the ServiceAccount's metadata.annotations.
                                type: object
                            type: object
                          bucket:
                            description: Bucket is the name of the S3 bucket.
                            type: string
                          endpoint:
                            description: Endpoint is the S3 endpoint URL. Useful for
                              S3-compatible stores like MinIO.
                            type: string
                          region:
                            description: Region is the AWS region of the bucket.
                            type: string
                          usePathStyle:
                            description: UsePathStyle forces path-style addressing
                              for S3-compatible providers that require it.
                            type: boolean
                        required:
                        - authentication
                        - bucket
                        type: object
                      timeoutSeconds:
                        description: 'NOTE: GCS, AzureBlob, etc., can be added here
                          in the future.'
                        type: integer
                    type: object
                  timeouts:
                    description: Timeouts
                    properties:
                      step:
                        description: Default timeout for individual steps
                        type: string
                      story:
                        description: Total time for the entire story
                        type: string
                    type: object
                  with:
                    description: With provides the inputs for the sub-story, mirroring
                      the 'with' field in an Engram step.
                    type: object
                    x-kubernetes-preserve-unknown-fields: true
                type: object
              steps:
                description: |-
                  Steps enumerates the workflow graph. Control-flow primitives and Engram
                  references are represented here.
                items:
                  description: |-
                    Step defines a node in the workflow graph. Nodes may refer to Engrams or to
                    built-in primitives such as condition, parallel, or executeStory.
                  properties:
                    execution:
                      description: |-
                        Execution carries per-step execution overrides. Prefer story- or template-level
                        configuration when possible.
                      properties:
                        automountServiceAccountToken:
                          default: true
                          description: AutomountServiceAccountToken toggles automountServiceAccountToken
                            on the pod spec.
                          type: boolean
                        debug:
                          description: Debug enables verbose, component-level logging
                            when true.
                          type: boolean
                        imagePullPolicy:
                          description: ImagePullPolicy overrides the container pull
                            policy.
                          type: string
                        maxInlineSize:
                          description: MaxInlineSize overrides the inline payload
                            threshold in bytes. Set to 0 to always offload.
                          type: integer
                        probes:
                          description: |-
                            Probes allows disabling template-defined probes; probe definitions themselves
                            remain owned by the template.
                          properties:
                            disableLiveness:
                              description: DisableLiveness disables the liveness probe
                                when true.
                              type: boolean
                            disableReadiness:
                              description: DisableReadiness disables the readiness
                                probe when true.
                              type: boolean
                            disableStartup:
                              description: DisableStartup disables the startup probe
                                when true.
                              type: boolean
                          type: object
                        retry:
                          description: Retry overrides the resolved retry policy.
                          properties:
                            backoff:
                              description: |-
                                Backoff strategy for retry delays
                                - exponential: 1s, 2s, 4s, 8s... (doubles each time)
                                - linear: 1s, 2s, 3s, 4s... (increases by delay each time)
                                - constant: 1s, 1s, 1s, 1s... (same delay each time)
                              enum:
                              - exponential
                              - linear
                              - constant
                              type: string
                            delay:
                              default: 1s
                              description: |-
                                Base delay between retries
                                Examples: "1s", "5s", "30s"
                              type: string
                            maxRetries:
                              default: 3
                              description: Maximum number of retry attempts
                              format: int32
                              maximum: 10
                              minimum: 0
                              type: integer
                          type: object
                        security:
                          description: Security overrides pod-level security settings.
                          properties:
                            allowPrivilegeEscalation:
                              description: Whether to allow privilege escalation
                              type: boolean
                            readOnlyRootFilesystem:
                              description: Read-only root filesystem
                              type: boolean
                            requiredSecrets:
                              description: Required secrets for this workload
                              items:
                                type: string
                              type: array
                            runAsNonRoot:
                              description: Whether to run as non-root user
                              type: boolean
                            runAsUser:
                              description: User ID to run as
                              format: int64
                              type: integer
                          type: object
                        serviceAccountName:
                          description: ServiceAccountName overrides the ServiceAccount
                            in use.
                          type: string
                        storage:
                          description: Storage overrides the resolved storage policy
                            (e.g., enable S3 or file providers).
                          properties:
                            file:
                              description: File specifies configuration for a filesystem-backed
                                object store.
                              properties:
                                emptyDir:
                                  description: |-
                                    EmptyDir defines an ephemeral volume to provision for file storage. When both
                                    EmptyDir and VolumeClaimName are omitted, an EmptyDir{} volume is used by default.
                                  properties:
                                    medium:
                                      description: |-
                                        medium represents what type of storage medium should back this directory.
                                        The default is "" which means to use the node's default medium.
                                        Must be an empty string (default) or Memory.
                                        More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                                      type: string
                                    sizeLimit:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: |-
                                        sizeLimit is the total amount of local storage required for this EmptyDir volume.
                                        The size limit is also applicable for memory medium.
                                        The maximum usage on memory medium EmptyDir would be the minimum value between
                                        the SizeLimit specified here and the sum of memory limits of all containers in a pod.
                                        The default is nil which means that the limit is undefined.
                                        More info: https://kubernetes.io/docs/concepts/storage/volumes#emptydir
                                      pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                      x-kubernetes-int-or-string: true
                                  type: object
                                path:
                                  description: |-
                                    Path is the directory inside the container where the storage backend should write files.
                                    Defaults to "/var/run/bubu/storage" when omitted.
                                  type: string
                                volumeClaimName:
                                  description: |-
                                    VolumeClaimName references an existing PersistentVolumeClaim mounted to the workload.
                                    Mutually exclusive with EmptyDir.
                                  type: string
                              type: object
                            s3:
                              description: S3 specifies the configuration for an S3-compatible
                                object store.
                              properties:
                                authentication:
                                  description: Authentication configures how the workload
                                    authenticates with S3.
                                  properties:
                                    secretRef:
                                      description: |-
                                        SecretRef references a Kubernetes Secret that contains the credentials.
                                        The secret must contain keys 'accessKeyID' and 'secretAccessKey'.
                                      properties:
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    serviceAccountAnnotations:
                                      additionalProperties:
                                        type: string
                                      description: |-
                                        ServiceAccountAnnotations allows specifying annotations to be added to the
                                        auto-generated ServiceAccount. This is the recommended approach for
                                        IAM-based authentication on cloud providers (e.g., IRSA on EKS).
                                        The key-value pairs will be added to the ServiceAccount's metadata.annotations.
                                      type: object
                                  type: object
                                bucket:
                                  description: Bucket is the name of the S3 bucket.
                                  type: string
                                endpoint:
                                  description: Endpoint is the S3 endpoint URL. Useful
                                    for S3-compatible stores like MinIO.
                                  type: string
                                region:
                                  description: Region is the AWS region of the bucket.
                                  type: string
                                usePathStyle:
                                  description: UsePathStyle forces path-style addressing
                                    for S3-compatible providers that require it.
                                  type: boolean
                              required:
                              - authentication
                              - bucket
                              type: object
                            timeoutSeconds:
                              description: 'NOTE: GCS, AzureBlob, etc., can be added
                                here in the future.'
                              type: integer
                          type: object
                        timeout:
                          description: Timeout overrides the resolved execution timeout
                            (Go duration string).
                          type: string
                      type: object
                    id:
                      description: |-
                        ID optionally provides a stable identifier distinct from Name. It is useful
                        for programmatic processing of the workflow graph.
                      maxLength: 63
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    if:
                      description: If gates execution on a CEL expression that evaluates
                        to true.
                      type: string
                    name:
                      description: |-
                        Name uniquely identifies the step within the Story. Outputs and dependencies
                        are referenced by this name.
                      maxLength: 63
                      minLength: 1
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    needs:
                      description: Needs lists predecessor step names that must complete
                        before this step starts.
                      items:
                        type: string
                      type: array
                    ref:
                      description: Ref points to the Engram executed by this step.
                      properties:
                        name:
                          description: |-
                            Name of the referenced object.
                            Must be a valid DNS-1123 label as defined by Kubernetes naming conventions.
                          maxLength: 253
                          minLength: 1
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        namespace:
                          description: |-
                            Namespace of the referenced object.
                            If empty, defaults to same namespace as the referencing object.

                            Cross-namespace references should be used carefully and only when
                            multi-tenancy is properly configured. Consider the security implications:
                            - Ensure RBAC policies allow cross-namespace access
                            - Validate that the target namespace is trusted
                            - Monitor cross-namespace references for security compliance
                          maxLength: 63
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          type: string
                        uid:
                          description: |-
                            UID is a type that holds unique ID values, including UUIDs.  Because we
                            don't ONLY use UUIDs, this is an alias to string.  Being a type captures
                            intent and helps make sure that UIDs and names do not get conflated.
                          type: string
                      required:
                      - name
                      type: object
                    runtime:
                      description: |-
                        Runtime carries dynamic per-packet configuration for realtime/streaming steps.
                        This field is ONLY used in realtime mode and is evaluated by the hub for each packet.

                        CEL expressions can reference:
                          - upstream.*: Outputs from predecessor steps (per-packet data)
                          - trigger.*: Story trigger inputs (static)
                          - packet.*: Current packet metadata

                        The hub evaluates this configuration for each packet and passes the result
                        in StreamMessage.Inputs to the engram.

                        Batch Mode: This field is ignored (use 'with' field instead)

                        Examples:
                          runtime: {userPrompt: "{{ upstream.transcribe.text }}", speakerId: "{{ packet.identity }}"}
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    secrets:
                      additionalProperties:
                        type: string
                      description: Secrets overrides template secret bindings for
                        Engram steps.
                      type: object
                    transport:
                      description: |-
                        Transport selects a named transport declared in Story.spec.transports. Streaming
                        steps use this to bind to the correct connector.
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    type:
                      description: |-
                        Type selects a built-in primitive. When omitted and Ref is set, the step
                        executes an Engram.
                      enum:
                      - condition
                      - parallel
                      - sleep
                      - stop
                      - switch
                      - filter
                      - transform
                      - wait
                      - throttle
                      - batch
                      - executeStory
                      - setData
                      - mergeData
                      - gate
                      type: string
                    with:
                      description: |-
                        With carries step configuration and parameters for the Engram.
                        Evaluation timing depends on execution mode:

                        Batch/Job Mode:
                          - Evaluated ONCE when the job starts
                          - Has access to story inputs (trigger.*) and outputs from completed predecessor steps (steps.*)
                          - Passed as BUBU_STEP_CONFIG environment variable

                        Realtime/Streaming Mode:
                          - Evaluated ONCE at deployment creation (static configuration)
                          - Has access to story inputs (trigger.*) only
                          - Step outputs are NOT available (they're per-packet, use 'runtime' field instead)
                          - Passed as BUBU_STEP_CONFIG environment variable

                        Examples:
                          Batch: with: {inputFile: "{{ steps.fetch.outputs.filename }}", config: "{{ trigger.settings }}"}
                          Realtime: with: {model: "gpt-4o-mini", systemPrompt: "{{ trigger.prompt }}"}
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                  required:
                  - name
                  type: object
                maxItems: 100
                minItems: 1
                type: array
              transports:
                description: |-
                  Transports declares named media/stream transports that steps can publish to
                  or subscribe from. Controllers surface these to StoryRuns so engrams know
                  whether to keep payloads on the transport ("hot") or fall back to storage.
                items:
                  description: StoryTransport describes a named transport binding
                    available to steps.
                  properties:
                    description:
                      description: Description provides optional human-readable context.
                      type: string
                    name:
                      description: Name references the transport from steps (e.g.
                        "realtime-audio").
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                      type: string
                    settings:
                      description: Settings contain provider-specific overrides evaluated
                        per StoryRun.
                      type: object
                      x-kubernetes-preserve-unknown-fields: true
                    transportRef:
                      description: TransportRef references the Transport CR providing
                        this functionality.
                      minLength: 1
                      type: string
                  required:
                  - name
                  - transportRef
                  type: object
                type: array
            required:
            - steps
            type: object
            x-kubernetes-validations:
            - message: each step must set exactly one of ref or type
              rule: self.steps.all(step, has(step.ref) != has(step.type))
            - message: step names must be unique
              rule: self.steps.all(step, self.steps.exists_one(other, other.name ==
                step.name))
          status:
            description: status defines the observed state of Story
            properties:
              conditions:
                description: |-
                  Standard Kubernetes conditions for detailed status tracking (e.g., "Ready", "Validated")
                  This will indicate if the Story's syntax is valid and all referenced Engrams exist.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  for this Story.
                format: int64
                type: integer
              stepsTotal:
                description: |-
                  StepsTotal reflects the total number of steps defined in the story's specification.
                  This provides a quick way to understand the complexity of the story.
                format: int32
                type: integer
              transports:
                description: Transports mirrors the declared transports with lightweight
                  readiness info.
                items:
                  description: StoryTransportStatus mirrors declared transports in
                    status.
                  properties:
                    mode:
                      description: |-
                        TransportMode captures how a transport is intended to be used in a Story.
                        "hot" transports remain in-band/low-latency, whereas "fallback" transports
                        act as spillover targets (e.g. object storage).
                      enum:
                      - hot
                      - fallback
                      type: string
                    modeReason:
                      description: ModeReason explains why the controller selected
                        the effective mode (e.g., "declarative-default", "requires-primitives").
                      type: string
                    name:
                      type: string
                    transportRef:
                      type: string
                  required:
                  - name
                  - transportRef
                  type: object
                type: array
              triggers:
                description: |-
                  Triggers reports how many StoryRuns currently reference this Story.
                  This approximates how frequently the story has been executed.
                format: int64
                type: integer
              usageCount:
                description: UsageCount reports how many Impulses reference this Story.
                format: int32
                type: integer
              validationErrors:
                description: ValidationErrors captures human-readable validation problems,
                  when present.
                items:
                  type: string
                type: array
              validationStatus:
                description: ValidationStatus indicates whether this Story's specification
                  passed validation.
                enum:
                - valid
                - invalid
                - unknown
                - pending
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
